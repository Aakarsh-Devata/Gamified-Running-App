import 'package:flutter/material.dart';
import '../widgets/run_tracker.dart';

class RunScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: RunTracker(),
    );
  }
}
